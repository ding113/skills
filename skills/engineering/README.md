# Engineering

Skills for daily code work.

Nothing promoted yet — the first candidates, `get-pr-comments` and `fix-ci`, are being drafted in [in-progress](../in-progress/README.md).

Once skills land here, this README lists each one with a one-line description, grouped into **User-invoked** and **Model-invoked** — see [.agents/invocation.md](../../.agents/invocation.md).
